import React from 'react';

import PublicTemplate from '../../templates/default';

const DiscoveryDetails = () => {

    return (
        <PublicTemplate>
            <h2>DiscoveryDetails</h2>
        </PublicTemplate>
    );
};

export default DiscoveryDetails;
